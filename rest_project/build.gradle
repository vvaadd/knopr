apply plugin: 'war'

sourceCompatibility = 1.5
version = '1.0'

dependencies {
    compile "org.glassfish.jersey.containers:jersey-container-servlet-core:2.9"
    compile project(":model_project")
    compile project(":core_project")
}