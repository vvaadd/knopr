apply plugin: "application"

version = '1.0'

mainClassName = "ru.knopr.Main"

dependencies {
    compile "org.slf4j:slf4j-simple:1.7.5"
    compile project(":library_project")
}

jar {
    manifest.attributes("Main-Class": mainClassName);
}